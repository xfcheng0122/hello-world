//Jenkinsfile (Declarative Pipeline)
pipeline {
    agent { node 'windows' }
    stages {
        stage('build') {
            steps {
                sh 'python --version'
            }
        }
    }
}
