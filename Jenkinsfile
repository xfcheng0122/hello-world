//Jenkinsfile (Declarative Pipeline)
pipeline {
    agent { centos8 'python:3.5.1' }
    stages {
        stage('build') {
            steps {
                sh 'python --version'
            }
        }
    }
}
