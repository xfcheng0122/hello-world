//Jenkinsfile (Declarative Pipeline)
pipeline {
    agent { node 'centos8' }
    stages {
        stage('build') {
            steps {
                sh 'python --version'
            }
        }
    }
}
