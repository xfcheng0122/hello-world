//Jenkinsfile (Declarative Pipeline)
pipeline {
    agent { node 'Built-In Node' }
    stages {
        stage('build') {
            steps {
                sh 'python --version'
            }
        }
    }
}
